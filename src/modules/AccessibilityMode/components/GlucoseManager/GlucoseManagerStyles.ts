import {StyleSheet} from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    logo: {
        flex: 1,
        justifyContent: 'center',
        padding: 15,
        alignItems: 'center'
    }
})
